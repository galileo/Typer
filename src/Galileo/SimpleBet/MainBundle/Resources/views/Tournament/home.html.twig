{% extends('GalileoSimpleBetMainBundle::layout.html.twig') %}

{% block content %}
        {% for tournament in tournaments %}
                <h4><a href="{{ path('gsbm_tournament_view', { 'tournamentId': tournament.id }) }}">
                    {{ tournament.name }}
                </a></h4>
        {% endfor %}
{% endblock %}


{% block sidebar %}
{{ render(controller('gsbm.controller.player:bestPlayerAction')) }}
{% endblock %}